name: Nightly Demo Update
env:
  SKIP_YARN_COREPACK_CHECK: true
  DEMO_BUILD_ID: ${{ github.run_number }}
  OKAREO_API_KEY: ${{ secrets.OKAREO_API_KEY }}
  OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
  
on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 7 * * *'

jobs:
  evaluation:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./demos
    permissions:
      contents: 'read'
      id-token: 'write'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Okareo Action
        uses: okareo-ai/okareo-action@v2.3
      
      - name: Run Nightly Demo Script
        run: |
          okareo -v
          okareo run -f summarization
  
  reporting:
    runs-on: ubuntu-latest
    if: always()
    needs: evaluation
    defaults:
      run:
        working-directory: ./demos
    permissions:
      contents: 'read'
      id-token: 'write'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Okareo Action
        uses: okareo-ai/okareo-action@v2.3
      
      - name: Run Nightly Demo Script
        run: |
          okareo -v
          okareo run -f x_summarization_logger


